syntax = "proto3";

package gastrolog.v1;

import "google/protobuf/timestamp.proto";
import "gastrolog/v1/system.proto";
import "gastrolog/v1/storage.proto";

option go_package = "gastrolog/api/gen/gastrolog/v1;gastrologv1";

// SystemCommand is a single mutation applied to the system store via Raft.
// Each variant maps 1:1 to a system.Store write method.
message SystemCommand {
  oneof command {
    // Tags 1 (PutFilterCommand) and 2 (DeleteFilterCommand) removed in
    // gastrolog-4kkoo (Phase 5): FilterConfig was inlined into
    // RouteConfig.MatchStage.expression. Remaining tags renumbered to
    // fill the holes per the project's "delete and renumber" rule.
    PutRotationPolicyCommand put_rotation_policy = 1;
    DeleteRotationPolicyCommand delete_rotation_policy = 2;
    PutRetentionPolicyCommand put_retention_policy = 3;
    DeleteRetentionPolicyCommand delete_retention_policy = 4;
    PutVaultCommand put_vault = 5;
    DeleteVaultCommand delete_vault = 6;
    PutIngesterCommand put_ingester = 7;
    DeleteIngesterCommand delete_ingester = 8;
    PutSettingCommand put_setting = 9;
    DeleteSettingCommand delete_setting = 10;
    PutCertificateCommand put_certificate = 11;
    DeleteCertificateCommand delete_certificate = 12;
    CreateUserCommand create_user = 13;
    UpdatePasswordCommand update_password = 14;
    UpdateUserRoleCommand update_user_role = 15;
    UpdateUsernameCommand update_username = 16;
    DeleteUserCommand delete_user = 17;
    InvalidateTokensCommand invalidate_tokens = 18;
    PutUserPreferencesCommand put_user_preferences = 19;
    CreateRefreshTokenCommand create_refresh_token = 20;
    DeleteRefreshTokenCommand delete_refresh_token = 21;
    DeleteUserRefreshTokensCommand delete_user_refresh_tokens = 22;
    PutNodeConfigCommand put_node_config = 23;
    DeleteNodeConfigCommand delete_node_config = 24;
    PutClusterTLSCommand put_cluster_tls = 25;
    PutRouteCommand put_route = 26;
    DeleteRouteCommand delete_route = 27;
    PutManagedFileCommand put_managed_file = 28;
    DeleteManagedFileCommand delete_managed_file = 29;
    PutCloudServiceCommand put_cloud_service = 30;
    DeleteCloudServiceCommand delete_cloud_service = 31;
    SetNodeStorageConfigCommand set_node_storage_config = 32;
    PutTierCommand put_tier = 33;
    DeleteTierCommand delete_tier = 34;
    SetTierPlacementsCommand set_tier_placements = 35;
    SetSetupWizardDismissedCommand set_setup_wizard_dismissed = 36;
    SetIngesterAliveCommand set_ingester_alive = 37;
    SetIngesterAssignmentCommand set_ingester_assignment = 38;
    SetIngesterCheckpointCommand set_ingester_checkpoint = 39;
  }
}

// --- Rotation Policies ---

message PutRotationPolicyCommand {
  bytes id = 1;
  string name = 2;
  optional string max_bytes = 3;
  optional string max_age = 4;
  optional int64 max_records = 5;
  optional string cron = 6;
}

message DeleteRotationPolicyCommand {
  bytes id = 1;
}

// --- Retention Policies ---

message PutRetentionPolicyCommand {
  bytes id = 1;
  string name = 2;
  optional string max_age = 3;
  optional string max_bytes = 4;
  optional int64 max_chunks = 5;
}

message DeleteRetentionPolicyCommand {
  bytes id = 1;
}

// --- Vaults ---

// PutVaultCommand carries the full vault config from system.proto. Embedded
// (rather than flattened) so the conversion layer is shared between the
// API and the FSM — matches PutCloudServiceCommand and
// SetNodeStorageConfigCommand pattern. Wire format break vs the previous
// flat {id, name, enabled} shape; acceptable per redesign decision 11.
message PutVaultCommand {
  VaultConfig vault = 1;
}

message DeleteVaultCommand {
  bytes id = 1;
  // When true, the vault's data directory is deleted from disk on the owning node.
  bool delete_data = 2;
}

// --- Ingesters ---

message PutIngesterCommand {
  bytes id = 1;
  string name = 2;
  string type = 3;
  bool enabled = 4;
  map<string, string> params = 5;
  bytes node_id = 6 [deprecated = true]; // Legacy. Use node_ids.
  repeated bytes node_ids = 7;           // Eligible nodes (honored when all_nodes=false).
  bool singleton = 8;                    // HA semantics: false = parallel, true = Raft-assigned singleton with failover.
  bool all_nodes = 9;                    // When true, eligibility is the entire current cluster (re-evaluated on membership change).
}

message DeleteIngesterCommand {
  bytes id = 1;
}

// --- Settings ---

message PutSettingCommand {
  string key = 1;
  string value = 2;
  // When non-empty, NotifySettingPut uses this value instead of key (which
  // stays "server" for the JSON blob). Empty preserves legacy "server" behavior.
  optional string notify_key = 3;
}

message DeleteSettingCommand {
  string key = 1;
}

// --- Certificates ---

message PutCertificateCommand {
  bytes id = 1;
  string name = 2;
  string cert_pem = 3;
  string key_pem = 4;
  string cert_file = 5;
  string key_file = 6;
}

message DeleteCertificateCommand {
  bytes id = 1;
}

// --- Users ---

message CreateUserCommand {
  bytes id = 1;
  string username = 2;
  string password_hash = 3;
  string role = 4;
  string preferences = 5;
  google.protobuf.Timestamp token_invalidated_at = 6;
  google.protobuf.Timestamp created_at = 7;
  google.protobuf.Timestamp updated_at = 8;
}

message UpdatePasswordCommand {
  bytes id = 1;
  string password_hash = 2;
}

message UpdateUserRoleCommand {
  bytes id = 1;
  string role = 2;
}

message UpdateUsernameCommand {
  bytes id = 1;
  string username = 2;
}

message DeleteUserCommand {
  bytes id = 1;
}

message InvalidateTokensCommand {
  bytes id = 1;
  google.protobuf.Timestamp at = 2;
}

message PutUserPreferencesCommand {
  bytes id = 1;
  string preferences = 2;
}

// --- Refresh Tokens ---

message CreateRefreshTokenCommand {
  bytes id = 1;
  bytes user_id = 2;
  string token_hash = 3;
  google.protobuf.Timestamp expires_at = 4;
  google.protobuf.Timestamp created_at = 5;
}

message DeleteRefreshTokenCommand {
  bytes id = 1;
}

message DeleteUserRefreshTokensCommand {
  bytes user_id = 1;
}

// --- Nodes ---

message PutNodeConfigCommand {
  bytes id = 1;
  string name = 2;
}

message DeleteNodeConfigCommand {
  bytes id = 1;
}

// --- Cluster TLS ---

message PutClusterTLSCommand {
  bytes ca_cert_pem = 1;
  bytes ca_key_pem = 2;
  bytes cluster_cert_pem = 3;
  bytes cluster_key_pem = 4;
  string join_token = 5;
}

// --- Routes ---

message PutRouteCommand {
  bytes id = 1;
  string name = 2;
  int32 priority = 3;
  repeated RouteStage stages = 4;
  repeated bytes destination_ids = 5;
  string distribution = 6;
  bool enabled = 7;
}

message DeleteRouteCommand {
  bytes id = 1;
}

// --- Managed Files ---

message PutManagedFileCommand {
  bytes id = 1;
  string name = 2;          // original filename
  string sha256 = 3;        // hex-encoded content hash
  int64 size = 4;           // file size in bytes
  string uploaded_at = 5;   // RFC3339 timestamp
}

message DeleteManagedFileCommand {
  bytes id = 1;
}

// --- Cloud Services ---

// PutCloudServiceCommand carries the full API CloudService from storage.proto.
// Previously flattened all fields here; now embeds to share the conversion layer.
// See gastrolog-2f8et. Breaking change: not wire-compatible with old Raft logs.
message PutCloudServiceCommand {
  CloudService cloud_service = 1;
}

message DeleteCloudServiceCommand {
  bytes id = 1;
}

// --- Node Storage ---

// SetNodeStorageConfigCommand carries the full API NodeStorageConfig from storage.proto.
message SetNodeStorageConfigCommand {
  NodeStorageConfig node_storage = 1;
}

// --- Tiers ---

// PutTierCommand carries the full API TierConfig from system.proto.
message PutTierCommand {
  TierConfig tier = 1;
}

message DeleteTierCommand {
  bytes id = 1;
  bool drain = 2; // When true, drain chunks to the next tier before deleting.
}

// --- Tier Placements ---

message SetTierPlacementsCommand {
  bytes tier_id = 1;
  repeated VaultPlacement placements = 2;
}

// --- Setup Wizard ---

message SetSetupWizardDismissedCommand {
  bool dismissed = 1;
}

// --- Ingester Runtime ---

message SetIngesterAliveCommand {
  bytes ingester_id = 1;
  string node_id = 2;
  bool alive = 3;
}

message SetIngesterAssignmentCommand {
  bytes ingester_id = 1;
  string node_id = 2; // Assigned node. Empty = unassigned.
}

message SetIngesterCheckpointCommand {
  bytes ingester_id = 1;
  bytes data = 2; // Opaque checkpoint blob.
}

// --- Snapshot ---

// SystemSnapshot captures the full system state for FSM.Snapshot()/Restore().
// Each repeated field contains one entry per entity, using the Put/Create
// command messages to represent complete entity state.
message SystemSnapshot {
  // Tag 1 (filters) removed in gastrolog-4kkoo. Remaining tags renumbered.
  repeated PutRotationPolicyCommand rotation_policies = 1;
  repeated PutRetentionPolicyCommand retention_policies = 2;
  repeated PutVaultCommand vaults = 3;
  repeated PutIngesterCommand ingesters = 4;
  map<string, string> settings = 5;
  repeated PutCertificateCommand certificates = 6;
  repeated CreateUserCommand users = 7;
  repeated CreateRefreshTokenCommand refresh_tokens = 8;
  repeated PutNodeConfigCommand node_configs = 9;
  PutClusterTLSCommand cluster_tls = 10;
  repeated PutRouteCommand routes = 11;
  repeated PutManagedFileCommand managed_files = 12;
  repeated PutCloudServiceCommand cloud_services = 13;
  repeated SetNodeStorageConfigCommand node_storage_configs = 14;
  repeated PutTierCommand tiers = 15;
  repeated SetTierPlacementsCommand tier_placements = 16;
  bool setup_wizard_dismissed = 17;
  repeated SetIngesterAliveCommand ingester_alive = 18;
  repeated SetIngesterAssignmentCommand ingester_assignments = 19;
  repeated SetIngesterCheckpointCommand ingester_checkpoints = 20;
}
